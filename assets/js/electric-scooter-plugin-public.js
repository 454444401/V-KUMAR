(function( $ ) {
    'use strict';

    // Add any public-facing JavaScript here.
    // For example, client-side form validation if needed.

    // $(document).ready(function() {
    // Example: Client-side validation for registration form
    // $('#electric-scooter-registration-form form').submit(function(event) {
    // var username = $('#username').val();
    // var email = $('#email').val();
    // var password = $('#password').val();
    // var errors = [];
    //
    // if (username.length < 5) {
    // errors.push('Username must be at least 5 characters long.');
    // }
    // if (!/\S+@\S+\.\S+/.test(email)) {
    // errors.push('Please enter a valid email address.');
    // }
    // if (password.length < 8) {
    // errors.push('Password must be at least 8 characters long.');
    // }
    //
    // if (errors.length > 0) {
    // event.preventDefault(); // Stop form submission
    // // Display errors - you'll need a dedicated error display area in your form HTML
    // var errorHtml = '<ul>';
    // for (var i = 0; i < errors.length; i++) {
    // errorHtml += '<li>' + errors[i] + '</li>';
    // }
    // errorHtml += '</ul>';
    // $('#form-errors').html(errorHtml).show(); // Assuming an element with id="form-errors"
    // } else {
    // $('#form-errors').hide();
    // }
    // });
    // });

})( jQuery );
